<%= form_with(model: item) do |form| %>
  <% if item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
        <% item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :max_borrowing_period, style: "display: block" %>
    <%= form.datetime_field :max_borrowing_period %>
  </div>
  
  <div>
      <br />
      <%= collection_select(:borrower_groups, :id, @all_groups, :id, :name, {:selected => Permission.borrowers_of_item(item.id)}, {:multiple => true}) %>
  </div>

  <div> <%= "Permissions" %> <br /> </div>
  <%= select_tag 'permission_group', options_for_select(Group.all.pluck(:name), prompt: 'Select a Group'), class: 'form-control' %>  

  <%#  <div class="dropdown">
    <button class="btn dropdown-toggle bg-primary text-white" href="#" type="button" id="dropdownPermissionButton" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
      Dropdown
    </button>

    <ul class="dropdown-menu" aria-labelledby="dropdownPermissionButton">
      <li> <a class="dropdown-item" href="#"> Action </a></li>
      <li> <a class="dropdown-item" href="#">Another action</a></li>
      <li><a class="dropdown-item" href="#">Something else here</a></li>
    </ul>
  </div> %>
  <br />

  <div>
    <%= form.submit %>
  </div>
<% end %>


