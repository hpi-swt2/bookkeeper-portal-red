<div class="row py-5 my-5 text-center">
    <%# Bootstrap icons: https://icons.getbootstrap.com/ %>
    <i class="bi bi-journal-bookmark-fill" style="font-size: 5rem; color: red;"></i>
    <h1 class="display-5 fw-bold">Bookkeeper Red</h1>
    <div class="col-lg-6 mx-auto">
        <p class="lead mb-4"><%= t('hello')%> Some text about this application's many features, the use cases as part of the sharing economy that it enables and the business value that users of the application can expect. Could also include a call to action to log in or could contain a rhetorical question. Wouldn't that be nice? </p>
        <div class="gap-3 d-sm-flex justify-content-sm-center">
            <%# https://github.com/heartcombo/devise#controller-filters-and-helpers %>
            <% if user_signed_in? %>
                <div class="row py-2">
                    <div class="col">
                        currently logged in user: <%= current_user.email %> |
                        <%= link_to edit_user_registration_path, class: "link-primary" do %>
                            <i class="bi bi-pencil-square"></i> Edit user profile
                        <% end %> | 
                        <%= link_to destroy_user_session_path, data: {"turbo-method": :delete}, class: "link-secondary" do %>
                            <i class="bi bi-door-open-fill"></i> Log out
                        <% end %>
                    </div>

                    <%= link_to "Profile", profiles_me_path %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
</div>
